services:
  redis:
    image: redis:7
    restart: always
    volumes:
      - redis-data:/data

  app:
    build: .
    restart: always
    ports:
      - "8123:8123"
    env_file:
      - .env
    depends_on:
      - redis

volumes:
  redis-data:
